from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A repository for eploring different methods for generating timeseries data for the case of limited training data',
    author='Milo Carroll',
    license='MIT',
)
